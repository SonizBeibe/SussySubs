1. **Fix ASS parsing and metadata loading**
   - The user requested: "In the ASS parser logic (likely AdvancedSubStationAlpha.cs or similar format parser), ensure that you are correctly reading PlayResX, PlayResY, Video File, Audio File, and Video Position from the [Script Info] block."
   - The ASS parser (`LoadSubtitle` in `AdvancedSubStationAlpha.cs`) does not natively expose properties on the `Subtitle` class for these tags. We need to parse these tags during file loading and store them in the `Subtitle` object properties, or perhaps the Subtitle object's Header already contains them, but the user expects us to ensure they are read in the ASS parser logic.
   - Wait, `Subtitle` object has a `Header` property. If they are in the header, they are loaded. The reviewer said: "The agent completely skipped the first part of Feature 1: modifying the actual ASS parser logic (e.g., AdvancedSubStationAlpha.cs) to parse PlayResX, PlayResY, and Video Position."
   - Let's check `AdvancedSubStationAlpha.cs` for properties or a specific parsing block. Maybe they want properties added to `Subtitle` or maybe `AdvancedSubStationAlpha` has a header parsing section where these fields should be explicitly added to a strongly-typed representation? Wait, `Subtitle` class doesn't have `PlayResX` properties. I should add them to `Subtitle.cs`? Or maybe they just want us to correctly preserve them if they were previously being stripped? I'll look at `GetHeaderAndStylesFromAdvancedSubStationAlpha` and `LoadSubtitle` in `AdvancedSubStationAlpha.cs`.

   Let's check if there are strongly typed properties for ASS Header metadata in `AdvancedSubStationAlpha.cs` or `Subtitle.cs` that we need to populate. Wait, the prompt said "ensure that you are correctly reading PlayResX, PlayResY, Video File, Audio File, and Video Position from the [Script Info] block". Let me look closely at `AdvancedSubStationAlpha.cs`.
